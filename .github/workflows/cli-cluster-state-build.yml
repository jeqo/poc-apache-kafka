name: 'cli:cluster-state:build'

on:
  push:
    branches: [ main ]
    paths:
      - 'cli/cluster-state/**'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    if: startsWith(github.event.head_commit.message, '🏁 Releasing version') != true && startsWith(github.event.head_commit.message, '⬆️  Next version') != true
    runs-on: ubuntu-latest

    steps:
      - name: 'Check out repository'
        uses: actions/checkout@v2

      - name: 'Set up Java'
        uses: actions/setup-java@v2
        with:
          java-version: 17
          distribution: 'zulu'

      - name: 'Cache Maven packages'
        uses: actions/cache@v2.1.7
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 'Build deps'
        working-directory: clients/
        run: ./mvnw -B --file pom.xml install

      - name: 'Build'
        working-directory: cli/cluster-state
        run: ./mvnw -B --file pom.xml verify